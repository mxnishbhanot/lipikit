import { PromptBuilder } from '../../../prompts/components/PromptBuilder.js';

/** The prompt builder is the whole page; the settings shell supplies the frame. */
export function PromptTemplatesPage(): JSX.Element {
  return <PromptBuilder />;
}
