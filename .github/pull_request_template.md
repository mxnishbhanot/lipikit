## What changed

<!-- One or two sentences. Link the issue if there is one: Fixes #123 -->

## Why

<!-- The problem this solves. Skip if the title already says it. -->

## Checks

- [ ] `pnpm lint` and `pnpm typecheck` pass
- [ ] `pnpm test` passes
- [ ] Tested on the platforms this touches (Windows / Linux), or it is platform-independent
- [ ] User-visible strings come from `packages/shared/src/config/branding.ts` — no hard-coded product name
- [ ] Docs updated if behaviour changed (README, `docs/`)

## Notes for the reviewer

<!-- Anything non-obvious: a trade-off taken, a corner deliberately cut. -->
